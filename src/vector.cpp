#include "include/vector.h"

/*WIP
vector<int> range(uint32_t count,int from,int step)
{
    vector<int> ret(count);
    for(uint32_t i = 0;i<count;i++)
    {
        ret[i]=from+step*i;
    }
    return ret;
}*/